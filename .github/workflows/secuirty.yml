name: Security CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  security-scan:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Install runtime (Python)
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 3. Build the application
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pipx install -r requirements.txt

      # 4. Generate SBOM
      - name: Install Syft
        run: curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | sh -s -- -b /usr/local/bin

      - name: Generate SBOM
        run: syft scan dir:. -o cyclonedx-json > sbom.json

      # 5. Run dependency (SCA) scan — fails the job on Critical severity
      - name: Install Grype
        run: curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b /usr/local/bin

      - name: Run Grype (fail on Critical)
        run: grype sbom:./sbom.json --fail-on critical

      # 6. Run SAST scan — Semgrep fails the job automatically when it finds issues
      - name: Run Semgrep
        run: |
          pip install semgrep
          semgrep --config auto --error
